{{title 'date-range-picker'}}

<h1>date-range-picker</h1>


{{date-picker value=date1.value minDate=date1.min maxDate=date1.max action=(action 'updateValue') range=true}}

<p>
  This component adds an easy to use date range picker.
</p>

<h2>Attributes</h2>

<table class="table table-bordered table-hover table-striped">
  <thead>
    <tr>
      <th>Attribute</th>
      <th>Description</th>
      <th>Default</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        dateFrom
      </td>
      <td>
        The default min. value for the date range picker.
      </td>
      <td>
        {{#code-inline language='javascript'}}null{{/code-inline}}
      </td>
    </tr>
    <tr>
      <td>
        dateTo
      </td>
      <td>
        The default max. value for the date range picker.
      </td>
      <td>
        {{#code-inline language='javascript'}}null{{/code-inline}}
      </td>
    </tr>
    <tr>
      <td>
        action
      </td>
      <td>
        The action to call whenever one of the dates changes.
        Has to be an actual action method, not an action name, e.g.
        {{#code-inline language='handlebars'}}action=(action 'myAction'){{/code-inline}}.
        The action will receive the selected (moment.js) dates as parameter. If one of the dates is not selected,
        the respective argument will be {{#code-inline language='javscript'}}null{{/code-inline}}
      </td>
      <td>
        {{#code-inline language='javascript'}}null{{/code-inline}}
      </td>
    </tr>
    <tr>
      <td>
        placeholderFrom
      </td>
      <td>
        The placeholder for the from field.
      </td>
      <td>
        {{#code-inline language='javascript' class='no-wrap'}}'Select From...'{{/code-inline}}
      </td>
    </tr>
    <tr>
      <td>
        placeholderTo
      </td>
      <td>
        The placeholder for the to field.
      </td>
      <td>
        {{#code-inline language='javascript' class='no-wrap'}}'Select To...'{{/code-inline}}
      </td>
    </tr>
  </tbody>
</table>

<h2>Basic Usage</h2>
\{{date-range-picker action=(action 'updateDateRange')}}
{{#code-block language='handlebars'}}\{{date-range-picker action=(action 'updateDateRange')}}{{/code-block}}