import { isEqualDay } from 'dummy/helpers/is-equal-day';
import { module, test } from 'qunit';

module('Unit | Helper | is equal day');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isEqualDay([42]);
  assert.ok(result);
});
