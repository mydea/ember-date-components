import { isEqualYear } from 'dummy/helpers/is-equal-year';
import { module, test } from 'qunit';

module('Unit | Helper | is equal year');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isEqualYear([42]);
  assert.ok(result);
});
